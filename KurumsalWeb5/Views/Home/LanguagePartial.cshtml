@model IEnumerable<KurumsalWeb5.Models.Model.Language>
@using KurumsalWeb5.Helper;

<ul class="dropdown-menu">
    @foreach (var item in Model)
    {<li>
    <a href="@Url.Action("ChangeLanguage", "Home", new { lang = item.Language_code })">@item.Baslik</a>
</li>
    }
       
</ul>
