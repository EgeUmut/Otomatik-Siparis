@model IEnumerable<KurumsalWeb5.Models.Model.Hizmet>
@{
    Layout = null;
}
<div class="col-lg-12">
    <h4 class="heading">@Resources.Lang.Our_Services</h4>
    <div class="row">
        <section id="projects">
            <ul id="thumbs" class="portfolio">
                @foreach (var item in Model)
                {
                    <!-- Item Project and Filter Name -->
                    <li class="col-lg-3 design" data-id="id-0" data-type="web">
                        <div class="item-thumbs">
                            <!-- Fancybox - Gallery Enabled - Title - Full Image -->
                            <a class="hover-wrap fancybox" data-fancybox-group="gallery" title="@item.Baslik" href="@item.ResimURL">
                                <span class="overlay-img"></span>
                                <span class="overlay-img-thumb font-icon-plus">@item.Baslik</span>
                            </a>
                            <!-- Thumb Image and Description -->
                            <img src="@item.ResimURL" alt="@item.Aciklama">
                        </div>
                    </li>

                }

            </ul>
        </section>
    </div>
</div>

